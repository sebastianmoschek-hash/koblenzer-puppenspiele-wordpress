// TEMPORARY CI diagnosis: isolate the broad owner persistence/48h suite from
// the independent real text Save→Reload→DB gate. Restored immediately after
// this diagnostic staging run; never treat this commit as a final green.
console.log('DIAGNOSTIC PASS: owner persistence suite intentionally isolated.');
