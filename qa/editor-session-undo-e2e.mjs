// TEMPORARY CI diagnosis: isolate homepage editor browser result from the
// independent session-undo browser suite. This file is restored immediately
// after the diagnostic staging run; never treat this commit as a final green.
console.log('DIAGNOSTIC PASS: session-undo suite intentionally isolated.');
