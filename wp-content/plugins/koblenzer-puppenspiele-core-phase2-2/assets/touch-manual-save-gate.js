(() => {
  'use strict';
  /*
   * Legacy compatibility file only.
   *
   * Older cached HTML may still reference this asset. It intentionally does
   * nothing: touch-gestures.js and touch-free-layout.js now keep local drafts
   * themselves, and touch-editor-bridge.js flushes them only from the explicit
   * orange Save button.
   */
})();
