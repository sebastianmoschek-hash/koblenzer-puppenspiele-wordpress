<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>Koblenzer Puppenspiele – Editor V2</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg: #0e0b09;
    --bg-panel: #17120f;
    --bg-elevated: #1f1712;
    --border: rgba(255,255,255,0.09);
    --border-strong: rgba(255,255,255,0.18);
    --text: #f3ede6;
    --text-dim: #b9ada1;
    --accent: #e07a35;
    --accent-2: #f2a559;
    --accent-soft: rgba(224,122,53,0.16);
    --danger: #d9534f;
    --ok: #6fbf73;
    --serif: 'Playfair Display', Georgia, serif;
    --sans: 'Inter', -apple-system, sans-serif;
    --radius: 14px;
    --shadow: 0 12px 30px rgba(0,0,0,0.45);
  }
  :root:not([data-theme="light"]){
    /* dark is our native/default look, values above already apply */
  }
  :root[data-theme="light"]{
    --bg: #f6f1ea;
    --bg-panel: #ffffff;
    --bg-elevated: #ffffff;
    --border: rgba(20,15,10,0.1);
    --border-strong: rgba(20,15,10,0.22);
    --text: #201810;
    --text-dim: #6c5f52;
    --accent: #c65f22;
    --accent-2: #e0863f;
    --accent-soft: rgba(198,95,34,0.14);
  }
  @media (prefers-color-scheme: light){
    :root:not([data-theme="dark"]){
      --bg: #f6f1ea;
      --bg-panel: #ffffff;
      --bg-elevated: #ffffff;
      --border: rgba(20,15,10,0.1);
      --border-strong: rgba(20,15,10,0.22);
      --text: #201810;
      --text-dim: #6c5f52;
      --accent: #c65f22;
      --accent-2: #e0863f;
      --accent-soft: rgba(198,95,34,0.14);
    }
  }

  *{ box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
  html,body{ margin:0; padding:0; background:var(--bg); color:var(--text); font-family:var(--sans); overflow-x:hidden; touch-action:pan-x pan-y; }
  img{ max-width:100%; display:block; }
  button{ font-family:inherit; cursor:pointer; }

  /* ===== App shell ===== */
  #app{ min-height:100vh; display:flex; flex-direction:column; }

  .topbar{
    position:sticky; top:0; z-index:60;
    display:flex; align-items:center; gap:6px;
    padding:10px 8px; background:var(--bg-panel);
    border-bottom:1px solid var(--border);
    backdrop-filter: blur(8px);
  }
  .topbar-mid{ min-width:0; flex:1 1 auto; overflow:hidden; margin-right:2px; }
  .icon-btn{
    width:38px; height:38px; border-radius:10px; border:1px solid var(--border);
    background:transparent; color:var(--text); display:flex; align-items:center; justify-content:center;
    font-size:18px; flex:none; -webkit-touch-callout:none; -webkit-user-select:none; user-select:none;
  }
  .icon-btn:active{ background:var(--accent-soft); }
  .topbar-title{ font-family:var(--serif); font-weight:700; font-size:14px; letter-spacing:.2px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
  .topbar-sub{ display:block; font-family:var(--sans); font-weight:500; font-size:10px; color:var(--text-dim); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }

  .seg{
    display:flex; background:var(--bg-elevated); border:1px solid var(--border); border-radius:10px; padding:2px; flex:none;
  }
  .seg button{
    border:none; background:transparent; color:var(--text-dim); font-size:14px; padding:6px 7px; border-radius:8px;
    -webkit-touch-callout:none; -webkit-user-select:none; user-select:none;
  }
  .seg button.active{ background:var(--accent); color:#1a0f06; }

  .edit-toggle{
    display:flex; align-items:center; gap:6px; padding:6px 8px 6px 10px; border-radius:999px;
    background:var(--bg-elevated); border:1px solid var(--border); flex:none;
  }
  .edit-toggle span{ font-size:11.5px; font-weight:600; color:var(--text-dim); white-space:nowrap; }
  @media (max-width:380px){
    .edit-toggle span{ display:none; }
    .edit-toggle{ padding:6px; }
  }
  .switch{ position:relative; width:40px; height:23px; border-radius:999px; background:var(--border-strong); border:none; padding:0; flex:none; transition:background .2s; }
  .switch::after{ content:''; position:absolute; top:2px; left:2px; width:19px; height:19px; border-radius:50%; background:#fff; transition:transform .2s; box-shadow:0 1px 3px rgba(0,0,0,.4); }
  .switch.on{ background:var(--accent); }
  .switch.on::after{ transform:translateX(17px); }

  /* ===== Editor mode banner ===== */
  .edit-banner{
    display:none; align-items:center; gap:8px; font-size:12px; font-weight:600;
    color:#1a0f06; background:linear-gradient(90deg,var(--accent-2),var(--accent));
    padding:7px 14px; text-align:center; justify-content:center;
  }
  body.editing .edit-banner{ display:flex; }

  /* ===== Stage / canvas frame ===== */
  .stage-wrap{ flex:1; display:flex; justify-content:center; background:var(--bg); padding:14px 0 90px; }
  .stage{
    width:100%; max-width:520px; background:var(--bg-panel);
    border:1px solid var(--border); border-radius:18px; overflow:hidden;
    box-shadow:var(--shadow); transition:max-width .25s ease; position:relative;
  }
  .stage.mobile{ max-width:390px; }
  .stage.desktop{ max-width:520px; }

  /* ===== Site content ===== */
  .site-nav{
    display:flex; align-items:center; gap:6px; padding:10px 12px; flex-wrap:wrap; row-gap:6px;
    background:linear-gradient(135deg,#1c1108,#2a160a); border-bottom:2px solid var(--accent);
    position:relative;
  }
  :root[data-theme="light"] .site-nav{ background:linear-gradient(135deg,#2a160a,#3a2010); }
  .brand{ display:flex; align-items:center; gap:8px; min-width:0; flex-shrink:0; }
  .brand-emoji{ font-size:20px; flex-shrink:0; height:32px; width:auto; object-fit:contain; }
  .brand-mark{
    height:32px; width:32px; border-radius:9px; display:flex; align-items:center; justify-content:center;
    background:linear-gradient(135deg,var(--accent-2),var(--accent)); color:#1a0f06; font-family:var(--serif);
    font-weight:800; font-size:13px; letter-spacing:.3px;
  }
  .brand-text{ min-width:0; }
  .brand-text b{ display:block; font-family:var(--serif); color:#fff; font-size:13px; line-height:1.15; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:150px; }
  .brand-text small{ display:block; color:var(--accent-2); font-size:9px; letter-spacing:.2px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:150px; }
  .nav-links{ display:flex; gap:10px; flex-wrap:wrap; row-gap:4px; width:100%; margin-left:0; padding-top:2px; border-top:1px solid rgba(255,255,255,0.08); margin-top:2px; }
  .nav-links a{ color:#e9dfd4; text-decoration:none; font-size:11.5px; font-weight:600; white-space:nowrap; }
  .stage.mobile .nav-links{ display:none; }
  .nav-burger{ display:none; color:#fff; font-size:20px; margin-left:auto; flex-shrink:0; }
  .stage.mobile .brand{ margin-right:auto; }

  .hero{
    position:relative; padding:34px 22px 30px; text-align:left;
    background:
      radial-gradient(circle at 85% 0%, rgba(224,122,53,0.25), transparent 55%),
      linear-gradient(180deg,#150d08,#0e0b09);
    border-bottom:1px solid var(--border);
  }
  :root[data-theme="light"] .hero{ background: radial-gradient(circle at 85% 0%, rgba(224,122,53,0.18), transparent 55%), linear-gradient(180deg,#fff,#f6f1ea); }
  .hero-eyebrow{ color:var(--accent-2); font-weight:700; font-size:11px; margin:0 0 10px; }
  .hero h1{ font-family:var(--serif); font-size:30px; line-height:1.08; margin:0 0 6px; color:var(--text); font-weight:800; }
  .hero h1 em{ font-style:normal; color:var(--accent); }
  .hero p{ color:var(--text-dim); font-size:14px; line-height:1.5; margin:0 0 18px; max-width:34ch; }
  .btn-cta{
    display:inline-block; background:var(--accent); color:#1a0f06; font-weight:700; font-size:14px;
    padding:12px 20px; border-radius:10px; border:none; text-decoration:none; margin-right:8px; margin-bottom:8px;
  }
  .btn-cta-outline{ background:transparent; border:2px solid #fff; color:#fff; }
  .puppet-scene{
    margin-top:22px; height:130px; border-radius:12px; position:relative; overflow:hidden;
    background:linear-gradient(160deg,#3a2412,#171009);
    display:flex; align-items:flex-end; justify-content:center;
  }
  .puppet-scene .stagecurtain{ position:absolute; inset:0; background:
      linear-gradient(90deg, rgba(160,30,20,.55) 0%, transparent 12%, transparent 88%, rgba(160,30,20,.55) 100%); }
  .puppet-scene .figure{ font-size:64px; z-index:2; filter:drop-shadow(0 8px 10px rgba(0,0,0,.5)); }
  .puppet-scene{ padding:0; }
  .puppet-scene .scene-photo{ position:absolute; inset:0; width:100%; height:100%; object-fit:cover; object-position:center 20%; }

  .stats-bar{ display:flex; border-bottom:1px solid var(--border); }
  .stats-bar div{ flex:1; text-align:center; padding:14px 4px; border-right:1px solid var(--border); }
  .stats-bar div:last-child{ border-right:none; }
  .stats-bar b{ display:block; font-family:var(--serif); font-size:18px; color:var(--accent-2); }
  .stats-bar span{ font-size:9.5px; color:var(--text-dim); text-transform:uppercase; letter-spacing:.4px; }

  .section{ padding:26px 20px; border-bottom:1px solid var(--border); }
  .section-title{ font-family:var(--serif); font-size:20px; margin:0 0 4px; font-weight:700; }
  .section-lead{ color:var(--text-dim); font-size:12.5px; margin:0 0 16px; }

  .program-grid{ display:grid; grid-template-columns:1fr 1fr; gap:10px; }
  .stage.mobile .program-grid{ grid-template-columns:1fr 1fr; }
  .prog-card{ background:var(--bg-elevated); border:1px solid var(--border); border-radius:12px; overflow:hidden; }
  .prog-thumb{ height:74px; display:flex; align-items:center; justify-content:center; font-size:30px; background:linear-gradient(150deg,#472a12,#20130a); position:relative; overflow:hidden; }
  .prog-thumb img{ position:absolute; inset:0; width:100%; height:100%; object-fit:cover; }
  .poster-card .prog-thumb{ height:150px; background:#0a0705; }
  .poster-card .prog-thumb img{ object-fit:contain; }
  .poster-card{ cursor:pointer; }
  .prog-card:nth-child(2n) .prog-thumb{ background:linear-gradient(150deg,#123424,#0d1a14); }
  .prog-card:nth-child(3n) .prog-thumb{ background:linear-gradient(150deg,#331636,#160a19); }
  .prog-info{ padding:9px 10px 11px; }
  .prog-info b{ display:block; font-size:12px; margin-bottom:2px; }
  .prog-info span{ font-size:10px; color:var(--text-dim); }

  .quote-block{ background:var(--bg-elevated); border-left:3px solid var(--accent); border-radius:10px; padding:16px; }
  .quote-block p{ font-family:var(--serif); font-style:italic; font-size:14.5px; margin:0 0 8px; }
  .quote-block cite{ font-size:11px; color:var(--text-dim); font-style:normal; }

  .contact-box{ display:flex; flex-direction:column; gap:8px; font-size:12.5px; color:var(--text-dim); }
  .contact-box b{ color:var(--text); }

  .news-item{ margin-bottom:16px; padding-bottom:16px; border-bottom:1px dashed var(--border); }
  .news-item:last-of-type{ border-bottom:none; }
  .news-item h3{ font-family:var(--serif); font-size:14.5px; margin:0 0 6px; color:var(--accent-2); }
  .news-item p{ font-size:12.5px; color:var(--text-dim); line-height:1.5; margin:0; }

  .event-grid{ display:flex; flex-direction:column; gap:10px; }
  .event-card{ display:flex; gap:12px; background:var(--bg-elevated); border-radius:12px; padding:12px; align-items:center; flex-wrap:wrap; }
  .event-date{ flex:none; width:52px; height:52px; border-radius:10px; background:var(--accent); color:#1a0f06; display:flex; flex-direction:column; align-items:center; justify-content:center; }
  .event-date b{ font-family:var(--serif); font-size:17px; line-height:1; }
  .event-date span{ font-size:9px; font-weight:700; text-transform:uppercase; }
  .event-info{ display:flex; flex-direction:column; gap:2px; min-width:0; flex:1; }
  .event-info b{ font-size:13px; }
  .event-info > span{ font-size:11.5px; color:va