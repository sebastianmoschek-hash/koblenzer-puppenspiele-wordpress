-radius:20px 20px 0 0; }
  @media (min-width:640px){ .detail-sheet{ border-radius:18px; } }
  .detail-sheet img{ width:100%; max-height:320px; object-fit:cover; display:block; }
  .detail-sheet-body{ padding:16px; }
  .detail-sheet-body h2{ font-family:var(--serif); font-size:19px; margin:0 0 12px; }
  .detail-meta-grid{ display:grid; grid-template-columns:1fr 1fr; gap:10px; margin-bottom:14px; }
  .detail-meta-item{ background:var(--bg-elevated); border-radius:10px; padding:9px 11px; }
  .detail-meta-item span{ display:block; font-size:9.5px; font-weight:700; color:var(--accent-2); text-transform:uppercase; letter-spacing:.3px; margin-bottom:3px; }
  .detail-meta-item b{ font-size:12.5px; }
  .detail-sheet-body p.desc{ font-size:13px; color:var(--text-dim); line-height:1.55; margin:0 0 16px; }
  .detail-sheet-actions{ display:flex; gap:10px; margin-bottom:10px; }
  .detail-poster-link{ display:block; text-align:center; font-size:11.5px; color:var(--text-dim); text-decoration:underline; margin-bottom:10px; }
  .detail-close-x{ position:sticky; top:0; float:right; margin:10px; width:32px; height:32px; border-radius:50%; background:rgba(0,0,0,.5); color:#fff; border:none; font-size:14px; z-index:2; }

  /* nav/menu editor list */
  .list-editor-row{ display:flex; align-items:center; gap:8px; padding:10px; background:var(--bg-elevated); border-radius:10px; margin-bottom:8px; }
  .list-editor-row input[type=text]{ flex:1; background:transparent; border:none; color:var(--text); font-size:13px; font-family:inherit; outline:none; }
  .list-editor-row .drag-dots{ color:var(--text-dim); font-size:14px; }
  .list-editor-row button{ background:transparent; border:none; color:var(--text-dim); font-size:15px; }
  .add-row-btn{ width:100%; padding:10px; border-radius:10px; border:1.5px dashed var(--border-strong); background:transparent; color:var(--text-dim); font-size:12.5px; font-weight:600; }

  /* Verlauf / Versionen */
  .history-row{ display:flex; align-items:center; gap:10px; padding:11px 12px; background:var(--bg-elevated); border-radius:10px; margin-bottom:8px; }
  .history-row.history-original{ border:1px solid var(--accent); }
  .history-row div{ flex:1; min-width:0; }
  .history-row b{ display:block; font-size:12.5px; }
  .history-row span{ display:block; font-size:10.5px; color:var(--text-dim); margin-top:2px; }
  .history-row button{ flex:none; background:var(--accent); color:#1a0f06; border:none; font-size:11px; font-weight:700; padding:8px 12px; border-radius:999px; }

  /* toast */
  #toast{
    position:fixed; left:50%; bottom:96px; transform:translateX(-50%) translateY(20px); opacity:0; z-index:400;
    background:var(--bg-elevated); color:var(--text); border:1px solid var(--border-strong); padding:10px 16px;
    border-radius:999px; font-size:12.5px; font-weight:600; display:flex; align-items:center; gap:8px; box-shadow:var(--shadow);
    transition:transform .25s ease, opacity .25s ease; pointer-events:none;
  }
  #toast.show{ opacity:1; transform:translateX(-50%) translateY(0); }
  #toast .dot{ width:7px; height:7px; border-radius:50%; background:var(--ok); }

  /* ===== Floating round menu button (mobile) ===== */
  .fab-menu{
    display:none; position:fixed; top:84px; right:max(14px, calc(50% - 175px)); z-index:90;
    height:44px; padding:0 16px 0 14px; border-radius:999px; border:none; gap:7px;
    background:linear-gradient(150deg,var(--accent-2),var(--accent)); color:#1a0f06;
    align-items:center; justify-content:center; font-size:14px; font-weight:700;
    box-shadow:0 8px 20px rgba(0,0,0,0.4); touch-action:none;
  }
  .fab-menu-ic{ font-size:16px; }
  .fab-menu-label{ font-size:12.5px; }
  @media (min-width:660px){ .fab-menu{ right:max(14px, calc(50% - 181px)); } }
  body.device-mobile .fab-menu{ display:flex; }
  .fab-menu.dragging{ opacity:.85; box-shadow:0 4px 30px rgba(0,0,0,0.6); }

  .fab-sheet-overlay{
    position:fixed; inset:0; background:rgba(6,4,2,0.72); z-index:250; display:none; align-items:flex-end; justify-content:center;
  }
  .fab-sheet-overlay.show{ display:flex; }
  .fab-sheet{
    width:100%; max-width:480px; background:var(--bg-panel); border:1px solid var(--border-strong);
    border-radius:20px 20px 0 0; max-height:80vh; overflow-y:auto; padding:8px 0 12px;
  }
  .fab-sheet-head{ display:flex; align-items:center; padding:14px 16px 6px; }
  .fab-sheet-head b{ font-family:var(--serif); font-size:15px; }
  .fab-sheet-head button{ margin-left:auto; width:30px; height:30px; border-radius:8px; border:1px solid var(--border); background:transparent; color:var(--text); }
  .fab-nav-item{
    display:flex; align-items:center; gap:10px; padding:13px 18px; font-size:13.5px; font-weight:600; color:var(--text);
    border-bottom:1px solid var(--border);
  }
  .fab-nav-item .dot{ width:6px; height:6px; border-radius:50%; background:var(--accent); flex:none; }
  .fab-nav-item .todo{ margin-left:auto; font-size:9.5px; color:var(--text-dim); font-weight:600; border:1px solid var(--border-strong); padding:2px 7px; border-radius:999px; }

  /* ===== Free move / pinch resize (long-press) ===== */
  body.editing [data-editable].free-drag-armed{ outline:2px solid var(--accent-2) !important; }
  body.editing [data-editable].free-dragging{ opacity:.88; z-index:120; box-shadow:0 14px 34px rgba(0,0,0,.5); cursor:grabbing; background:var(--bg-panel); }
  .free-drag-placeholder{ visibility:hidden; }
  .free-hint{
    position:fixed; left:50%; bottom:140px; transform:translateX(-50%); z-index:401; background:var(--bg-elevated);
    border:1px solid var(--border-strong); color:var(--text); font-size:11.5px; font-weight:600; padding:8px 14px; border-radius:999px;
    opacity:0; pointer-events:none; transition:opacity .2s; box-shadow:var(--shadow);
  }
  .free-hint.show{ opacity:1; }

  ::selection{ background:var(--accent-soft); }
</style>
</head>
<body>
<div id="app">

  <div class="topbar">
    <button class="icon-btn" id="btnBack" title="Zurück" aria-label="Zurück">‹</button>
    <div class="topbar-mid">
      <span class="topbar-title">Koblenzer Puppenspiele</span>
      <span class="topbar-sub" id="modeLabel">Live-Ansicht</span>
    </div>

    <div class="seg" id="deviceSeg" role="tablist" aria-label="Ansicht">
      <button data-device="desktop" class="active" title="Desktop-Ansicht">🖥</button>
      <button data-device="mobile" title="Mobile-Ansicht">📱</button>
    </div>

    <button class="icon-btn" id="themeBtn" title="Hell/Dunkel umschalten" aria-label="Hell/Dunkel umschalten">🌙</button>

    <div class="edit-toggle">
      <span>Bearbeiten</span>
      <button class="switch" id="editSwitch" aria-pressed="false" aria-label="Bearbeitungsmodus umschalten"></button>
    </div>
  </div>

  <div class="edit-banner">✏️ Editor aktiv — tippe auf Text, Bilder oder Buttons, um sie direkt zu ändern</div>

  <div class="stage-wrap">
    <div class="stage desktop" id="stage">

      <!-- NAV -->
      <nav class="site-nav">
        <div class="brand">
          <span class="brand-emoji brand-mark" data-editable="text" contenteditable="false">KP</span>
          <div class="brand-text">
            <b data-editable="text" data-label="Name" contenteditable="false">Koblenzer Puppenspiele</b>
            <small data-editable="text" data-label="Claim" contenteditable="false">Theater · Fantasie · Für Groß und Klein</small>
          </div>
        </div>
        <div class="nav-links" id="navLinks">
          <a href="#" data-anchor="heroSection" data-editable="text" contenteditable="false">Home</a>
          <a href="#" data-anchor="jetztBuchenSection" data-editable="text" contenteditable="false">Jetzt buchen!</a>
          <a href="#" data-anchor="aktuellSection" data-editable="text" contenteditable="false">Aktuell</a>
          <a href="#" data-anchor="progSection" data-editable="text" contenteditable="false">Programm</a>
          <a href="#" data-anchor="tourplanSection" data-editable="text" contenteditable="false">Tourplan</a>
          <a href="#" data-anchor="presseSection" data-editable="text" contenteditable="false">Presse</a>
          <a href="#" data-anchor="ensembleSection" data-editable="text" contenteditable="false">Ensemble</a>
          <a href="#" data-anchor="foerderungSection" data-editable="text" contenteditable="false">Förderung</a>
          <a href="#" data-anchor="kontaktSection" data-editable="text" contenteditable="false">Kontakt / Impressum</a>
          <a href="#" data-anchor="referenzenSection" data-editable="text" contenteditable="false">Referenzen</a>
          <a href="#" data-anchor="faqSection" data-editable="text" contenteditable="false">Kita / Schule FAQ</a>
          <a href="#" data-anchor="kirmesSection" data-editable="text" contenteditable="false">KIRMES-KASPER</a>
        </div>
        <button class="nav-burger" id="navEditBtn" data-editable="image" data-label="Menü" data-imgkey="nav">☰</button>
      </nav>

      <!-- HERO -->
      <section class="hero" id="heroSection">
        <div class="section-controls"><button class="sc-btn" data-move="up">↑</button><button class="sc-btn" data-move="down">↓</button></div>
        <p class="hero-eyebrow" data-editable="text" contenteditable="false">Live in Koblenz &amp; auf Tour</p>
        <h1 data-editable="text" contenteditable="false">Koblenzer <em>Puppenspiele</em></h1>
        <p data-editable="text" contenteditable="false">Wir sind ein mobiles und professionelles Figurentheater und kommen gerne zu Euch in die Kita, die Grundschule oder überall dorthin, wo man Lust auf frisches und lebendiges Puppenspiel verspürt.</p><p data-editable="text" contenteditable="false" style="font-size:11.5px;color:var(--text-dim)">Unsere Arbeit wurde bereits mit dem Kinder- und Jugendkulturpreis und dem Kulturpreis der SGK Rheinland-Pfalz geehrt. Wir sind Mitglied der Unima.</p>
        <a href="#" class="btn-cta" data-editable="text" contenteditable="false">Unser Programm entdecken →</a>
        <a href="#" class="btn-cta btn-cta-outline" id="heroBookBtn" data-editable="text" contenteditable="false">Vorstellung buchen</a>

        <div class="puppet-scene" data-editable="image" data-label="Bühnenbild" data-imgkey="hero">
          <img class="scene-photo" src="assets/header.webp" alt="Björn Christian Küpper mit Marionette">
          <div class="img-edit-badge">🖌 Bild bearbeiten</div>
        </div>
      </section>

      <!-- STATS -->
      <div class="stats-bar">
        <div><b data-editable="text" contenteditable="false">100+</b><span data-editable="text" contenteditable="false">Geförderte Gastspiele</span></div>
        <div><b data-editable="text" contenteditable="false">13</b><span data-editable="text" contenteditable="false">Stücke im Programm</span></div>
        <div><b data-editable="text" contenteditable="false">RLP</b><span data-editable="text" contenteditable="false">Landesweit auf Tour</span></div>
      </div>

      <!-- NÄCHSTE VORSTELLUNGEN -->
      <section class="section" id="naechsteSection">
        <div class="section-controls"><button class="sc-btn" data-move="up">↑</button><button class="sc-btn" data-move="down">↓</button></div>
        <h2 class="section-title" data-editable="text" contenteditable="false">Nächste Vorstellungen</h2>
        <p class="section-lead" data-editable="text" contenteditable="false">Eine Auswahl aus unserem echten Tourplan – öffentliche Termine zum Reinschauen und Mitkommen.</p>
        <div class="event-grid" id="eventGrid"></div>
        <button class="btn-outline" id="viewFullTourplanBtn" style="margin-top:16px;width:100%;padding:12px">Kompletten Tourplan ansehen →</button>
      </section>

      <!-- JETZT BUCHEN -->
      <section class="section" id="jetztBuchenSection">
        <div class="section-controls"><button class="sc-btn" data-move="up">↑</button><button class="sc-btn" data-move="down">↓</button></div>
        <h2 class="section-title" data-editable="text" contenteditable="false">Jetzt buchen!</h2>
        <p class="section