r(--text-dim); }
  .event-tag{ font-size:10px !important; color:var(--accent-2) !important; font-weight:600; margin-top:2px; }
  .event-ticket-btn{ flex:none; background:var(--accent); color:#1a0f06; border:none; font-size:10.5px; font-weight:700; padding:8px 12px; border-radius:999px; margin-left:auto; }

  .tour-wrap{ display:flex; flex-direction:column; gap:14px; max-height:420px; overflow-y:auto; padding-right:4px; }
  .tour-month h3{ font-family:var(--serif); font-size:13.5px; color:var(--accent-2); margin:0 0 8px; position:sticky; top:0; background:var(--bg-panel); padding:4px 0; }
  .tour-list{ display:flex; flex-direction:column; gap:5px; }
  .tour-row{ font-size:11.5px; color:var(--text-dim); background:var(--bg-elevated); padding:7px 10px; border-radius:8px; line-height:1.4; }

  .legal-details{ margin-top:14px; border-top:1px solid var(--border); padding-top:12px; }
  .legal-details summary{ font-size:12px; font-weight:600; color:var(--accent-2); cursor:pointer; }
  .legal-text{ margin-top:10px; }
  .legal-text h4{ font-size:12px; margin:12px 0 4px; color:var(--text); }
  .legal-text p{ font-size:11px; color:var(--text-dim); line-height:1.5; margin:0; }

  .ref-grid{ display:flex; flex-wrap:wrap; gap:8px; }
  .ref-chip{ background:var(--bg-elevated); border:1px solid var(--border); color:var(--text-dim); font-size:11.5px; font-weight:600; padding:8px 12px; border-radius:999px; }

  .faq-list{ display:flex; flex-direction:column; gap:8px; }
  .faq-list details{ background:var(--bg-elevated); border-radius:10px; padding:10px 12px; }
  .faq-list summary{ font-size:12.5px; font-weight:600; cursor:pointer; }
  .faq-list p{ font-size:12px; color:var(--text-dim); margin:8px 0 0; line-height:1.5; }

  .ensemble-bios{ display:flex; flex-direction:column; gap:8px; margin-top:14px; }
  .bio-card{ background:var(--bg-elevated); border-radius:10px; padding:11px 13px; }
  .bio-card summary{ font-size:12.5px; font-weight:700; cursor:pointer; color:var(--accent-2); }
  .bio-card p{ font-size:11.5px; color:var(--text-dim); margin:8px 0 0; line-height:1.5; }

  .badge-row{ display:flex; flex-wrap:wrap; gap:12px; align-items:center; }
  .badge-tile{ background:#fff; border-radius:10px; padding:8px 12px; display:flex; align-items:center; justify-content:center; height:52px; }
  .badge-tile img{ max-height:36px; max-width:120px; object-fit:contain; }

  .site-footer{ padding:20px; text-align:center; background:var(--bg-elevated); }
  .site-footer .brand{ justify-content:center; margin-bottom:6px; }
  .site-footer .brand-emoji{ height:44px; }
  .site-footer .brand-mark{ height:44px; width:44px; font-size:16px; }
  .site-footer p{ color:var(--text-dim); font-size:11px; margin:0; }

  /* ===== Editable element affordances ===== */
  body.editing [data-editable]{
    outline:1.5px dashed transparent; outline-offset:2px; border-radius:6px; position:relative; transition:outline-color .12s, background .12s;
    -webkit-touch-callout:none;
  }
  body.editing [data-editable]:not([contenteditable="true"]){ -webkit-user-select:none; user-select:none; }
  body.editing [data-editable]:hover, body.editing [data-editable].selected{
    outline-color:var(--accent);
    background:var(--accent-soft);
  }
  body.editing [data-editable]::before{
    content:attr(data-label); position:absolute; top:-9px; left:6px; font-size:8.5px; font-weight:700;
    background:var(--accent); color:#1a0f06; padding:1px 5px; border-radius:5px; letter-spacing:.3px;
    opacity:0; pointer-events:none; transition:opacity .12s; z-index:5;
  }
  body.editing [data-editable]:hover::before, body.editing [data-editable].selected::before{ opacity:1; }
  [data-editable="text"][contenteditable="true"]{ outline-color:var(--accent) !important; background:var(--accent-soft); cursor:text; }

  body.editing [data-editable="image"]{ cursor:pointer; }
  body.editing [data-editable="image"] .img-edit-badge{
    position:absolute; bottom:6px; right:6px; background:rgba(20,12,6,0.75); color:#fff; font-size:9px; font-weight:700;
    padding:4px 7px; border-radius:6px; display:flex; align-items:center; gap:4px; z-index:4;
  }
  body.editing [data-editable="image"] .img-edit-badge.compact{
    bottom:4px; right:4px; padding:3px 5px; font-size:10px;
  }
  [data-editable="image"] .img-edit-badge{ opacity:0; transition:opacity .15s; }
  body.editing [data-editable="image"]:hover .img-edit-badge{ opacity:1; }

  /* section drag handles */
  body.editing .section, body.editing .hero, body.editing .stats-bar{ position:relative; }
  .section-controls{
    display:none; position:absolute; top:6px; right:6px; z-index:8; gap:4px;
  }
  body.editing .section:hover > .section-controls,
  body.editing .section-controls.pinned{ display:flex; }
  .sc-btn{ width:26px; height:26px; border-radius:7px; background:var(--bg-panel); border:1px solid var(--border-strong); color:var(--text); font-size:12px; display:flex; align-items:center; justify-content:center; -webkit-touch-callout:none; -webkit-user-select:none; user-select:none; }
  .section[draggable="true"]{ cursor:grab; }
  .section.drag-over{ outline:2px dashed var(--accent); outline-offset:-4px; }
  .section.dragging{ opacity:.4; }

  /* ===== Floating text toolbar ===== */
  .text-toolbar{
    position:fixed; z-index:200; display:none; gap:3px; background:var(--bg-elevated);
    border:1px solid var(--border-strong); border-radius:10px; padding:4px; box-shadow:var(--shadow);
  }
  .text-toolbar.show{ display:flex; }
  .text-toolbar button{ width:30px; height:30px; border:none; background:transparent; color:var(--text); border-radius:6px; font-size:13px; }
  .text-toolbar button:active, .text-toolbar button.active{ background:var(--accent-soft); color:var(--accent-2); }

  /* ===== Bottom action bar (mobile-first, big touch targets) ===== */
  .bottom-bar{
    position:fixed; left:0; right:0; bottom:0; z-index:70;
    display:none; align-items:center; justify-content:space-between; gap:8px;
    padding:10px 14px calc(10px + env(safe-area-inset-bottom));
    background:var(--bg-panel); border-top:1px solid var(--border);
  }
  body.editing .bottom-bar{ display:flex; }
  .bb-group{ display:flex; gap:6px; }
  .bb-btn{
    display:flex; flex-direction:column; align-items:center; gap:2px; background:transparent; border:none;
    color:var(--text-dim); font-size:9.5px; font-weight:600; padding:6px 10px; border-radius:10px; min-width:52px;
    -webkit-touch-callout:none; -webkit-user-select:none; user-select:none;
  }
  .bb-btn span.ic{ font-size:18px; }
  .bb-btn.primary{ background:var(--accent); color:#1a0f06; }
  .bb-btn:active{ background:var(--accent-soft); }

  /* ===== Modal base ===== */
  .modal-overlay{
    position:fixed; inset:0; background:rgba(6,4,2,0.72); z-index:300; display:none;
    align-items:flex-end; justify-content:center; backdrop-filter: blur(2px);
  }
  .modal-overlay.show{ display:flex; }
  @media (min-width:640px){ .modal-overlay{ align-items:center; } }
  .modal-sheet{
    width:100%; max-width:480px; background:var(--bg-panel); border:1px solid var(--border-strong);
    border-radius:20px 20px 0 0; max-height:88vh; overflow-y:auto; animation:slideUp .22s ease;
  }
  @media (min-width:640px){ .modal-sheet{ border-radius:18px; } }
  @keyframes slideUp{ from{ transform:translateY(24px); opacity:0;} to{ transform:none; opacity:1;} }
  .modal-head{ display:flex; align-items:center; gap:10px; padding:14px 16px; border-bottom:1px solid var(--border); position:sticky; top:0; background:var(--bg-panel); }
  .modal-head b{ font-size:14.5px; font-family:var(--serif); }
  .modal-close{ margin-left:auto; width:30px; height:30px; border-radius:8px; border:1px solid var(--border); background:transparent; color:var(--text); }
  .modal-body{ padding:16px; }

  /* image editor */
  .img-preview-wrap{ border-radius:12px; overflow:hidden; margin-bottom:16px; background:#000; height:190px; display:flex; align-items:center; justify-content:center; position:relative; }
  .img-preview-wrap .prev-figure{ font-size:88px; }
  .crop-frame{ position:absolute; inset:14px 40px; border:2px dashed var(--accent-2); border-radius:8px; display:none; }
  .crop-frame.show{ display:block; }
  .tabs{ display:flex; gap:6px; margin-bottom:14px; background:var(--bg-elevated); padding:3px; border-radius:10px; }
  .tabs button{ flex:1; border:none; background:transparent; color:var(--text-dim); font-size:12px; font-weight:600; padding:8px 4px; border-radius:8px; }
  .tabs button.active{ background:var(--accent); color:#1a0f06; }
  .tabpanel{ display:none; }
  .tabpanel.active{ display:block; }
  .slider-row{ margin-bottom:14px; }
  .slider-row .lbl{ display:flex; justify-content:space-between; font-size:12px; color:var(--text-dim); margin-bottom:6px; }
  .slider-row .lbl b{ color:var(--accent-2); font-weight:700; }
  input[type=range]{ width:100%; accent-color:var(--accent); height:20px; }
  .filter-grid{ display:grid; grid-template-columns:repeat(4,1fr); gap:8px; }
  .filter-swatch{ border-radius:10px; overflow:hidden; border:2px solid transparent; background:var(--bg-elevated); text-align:center; padding-bottom:6px; }
  .filter-swatch.active{ border-color:var(--accent); }
  .filter-swatch .sw{ height:44px; display:flex; align-items:center; justify-content:center; font-size:20px; }
  .filter-swatch span{ font-size:9px; color:var(--text-dim); }
  .modal-actions{ display:flex; gap:10px; margin-top:6px; }
  .modal-actions button{ flex:1; padding:12px; border-radius:10px; font-weight:700; font-size:13px; border:1px solid var(--border-strong); background:transparent; color:var(--text); }
  .modal-actions .apply{ background:var(--accent); color:#1a0f06; border:none; }

  /* poster lightbox */
  .poster-modal-overlay{ position:fixed; inset:0; background:rgba(4,3,2,0.9); z-index:320; display:none; align-items:center; justify-content:center; padding:24px 14px; }
  .poster-modal-overlay.show{ display:flex; }
  .poster-modal-inner{ max-width:480px; width:100%; text-align:center; }
  .poster-modal-inner img{ max-width:100%; max-height:78vh; border-radius:10px; box-shadow:0 20px 50px rgba(0,0,0,.6); }
  .poster-modal-close{ margin:14px auto 0; display:block; width:40px; height:40px; border-radius:50%; border:1px solid var(--border-strong); background:var(--bg-elevated); color:#fff; font-size:16px; }

  /* new show cards (photo + meta + CTAs) */
  .show-grid{ display:flex; flex-direction:column; gap:16px; }
  .show-card{ background:var(--bg-elevated); border:1px solid var(--border); border-radius:14px; overflow:hidden; }
  .show-card-img{ width:100%; aspect-ratio:1/1; background:#0a0705; position:relative; overflow:hidden; }
  .show-card-img img{ width:100%; height:100%; object-fit:cover; }
  .show-card-body{ padding:14px 16px 16px; }
  .show-card-body h3{ font-family:var(--serif); font-size:16px; margin:0 0 8px; font-weight:700; }
  .show-pills{ display:flex; gap:6px; margin-bottom:10px; flex-wrap:wrap; }
  .pill{ background:var(--accent-soft); color:var(--accent-2); font-size:10.5px; font-weight:700; padding:5px 10px; border-radius:999px; }
  .show-card-body p{ font-size:12.5px; color:var(--text-dim); line-height:1.5; margin:0 0 14px; }
  .show-card-actions{ display:flex; gap:10px; }
  .btn-outline{ flex:1; background:transparent; border:2px solid #fff; color:#fff; font-size:13px; font-weight:700; padding:14px 10px; border-radius:999px; }
  .btn-solid{ flex:1; background:var(--accent); border:none; color:#1a0f06; font-size:13px; font-weight:700; padding:14px 10px; border-radius:999px; }

  /* show detail sheet */
  .detail-sheet-overlay{ position:fixed; inset:0; background:rgba(4,3,2,0.75); z-index:330; display:none; align-items:flex-end; justify-content:center; }
  .detail-sheet-overlay.show{ display:flex; }
  @media (min-width:640px){ .detail-sheet-overlay{ align-items:center; } }
  .detail-sheet{ width:100%; max-width:480px; max-height:88vh; overflow-y:auto; background:var(--bg-panel); border:1px solid var(--border-strong); border