(()=>{
'use strict';
const editing=()=>document.body.classList.contains('editing');let selected=null,gesture=null;
const num=(v,d)=>Number.isFinite(parseFloat(v))?parseFloat(v):d;
function apply(el){const scale=num(el.dataset.kpImageScale,1),rot=num(el.dataset.kpImageRotation,0),fx=num(el.dataset.kpImageFlipX,1),fy=num(el.dataset.kpImageFlipY,1);el.style.setProperty('transform',`scale(${scale*fx},${scale*fy}) rotate(${rot}deg)`,'important');el.style.setProperty('transform-origin','center center','important');el.style.setProperty('z-index','1','important')}
function select(el){selected=el&&el.tagName==='IMG'?el:null;if(selected)apply(selected)}
const dist=(a,b)=>Math.hypot(b.clientX-a.clientX,b.clientY-a.clientY),angle=(a,b)=>Math.atan2(b.clientY-a.clientY,b.clientX-a.clientX)*180/Math.PI;
function start(e){if(!editing())return;const img=e.target.closest('img');if(!img)return;const touches=e.touches;if(!touches||touches.length!==2)return;select(img);gesture={img,startDist:Math.max(1,dist(touches[0],touches[1])),startAngle:angle(touches[0],touches[1]),scale:num(img.dataset.kpImageScale,1),rotation:num(img.dataset.kpImageRotation,0),done:window.__kpUndoCheckpoint?.()};document.body.classList.add('kp-image-gesture-active');e.preventDefault()}
function move(e){if(!gesture||e.touches.length!==2)return;const t=e.touches,s=Math.max(.05,gesture.scale*dist(t[0],t[1])/gesture.startDist),r=gesture.rotation+(angle(t[0],t[1])-gesture.startAngle);gesture.img.dataset.kpImageScale=String(s);gesture.img.dataset.kpImageRotation=String(r);apply(gesture.img);e.preventDefault()}
function end(e){if(!gesture)return;if(e.touches&&e.touches.length>=2)return;gesture.done?.();gesture=null;document.body.classList.remove('kp-image-gesture-active');window.markDirty?.('Bild mit Zwei-Finger-Geste geändert – noch speichern')}
document.addEventListener('touchstart',start,{passive:false,capture:true});document.addEventListener('touchmove',move,{passive:false,capture:true});document.addEventListener('touchend',end,{passive:false,capture:true});document.addEventListener('touchcancel',end,{passive:false,capture:true});window.addEventListener('kp-element-selected',e=>select(e.detail?.element));window.addEventListener('kp-editor-restored',()=>{gesture=null;selected=null;document.body.classList.remove('kp-image-gesture-active');document.querySelectorAll('img[data-kp-image-scale],img[data-kp-image-rotation],img[data-kp-image-flip-x],img[data-kp-image-flip-y]').forEach(apply)});window.kpImageGestures={apply,select};
})();