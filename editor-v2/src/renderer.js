const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
function element(e){if(e.type==='heading')return `<h1 data-node="${esc(e.id)}">${esc(e.text)}</h1>`;if(e.type==='text')return `<p data-node="${esc(e.id)}">${esc(e.text)}</p>`;return ''}
export function renderDocument(doc,root){const page=doc.pages[0];root.innerHTML=`<header class="site-header" data-node="site-header"><strong>${esc(doc.site.title)}</strong><nav>${doc.navigation.map(n=>`<button data-node="${esc(n.id)}">${esc(n.label)}</button>`).join('')}</nav></header><main>${page.sections.length?page.sections.map(s=>`<section data-node="${esc(s.id)}">${(s.children||[]).map(element).join('')}</section>`).join(''):'<section class="empty"><h1>Editor V2</h1><p>Dokumentmodell bereit.</p></section>'}</main>`;}