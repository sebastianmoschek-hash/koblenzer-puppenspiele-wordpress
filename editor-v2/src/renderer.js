const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
export function renderDocument(doc,root){const page=doc.pages[0];root.innerHTML=`<header class="site-header"><strong>${esc(doc.site.title)}</strong><nav>${doc.navigation.map(n=>`<button data-node="${esc(n.id)}">${esc(n.label)}</button>`).join('')}</nav></header><main>${page.sections.length?page.sections.map(s=>`<section data-node="${esc(s.id)}"><h2>${esc(s.title||'Abschnitt')}</h2></section>`).join(''):'<section class="empty"><h1>Editor V2</h1><p>Das neue Dokumentmodell läuft. Als Nächstes werden die Originalinhalte der Koblenzer Puppenspiele migriert.</p></section>'}</main>`;}